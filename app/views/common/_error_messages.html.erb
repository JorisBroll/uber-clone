<% if object.errors.any? %>
  <div class="alert alert-danger">
    <p>
      Le formulaire contient <%= pluralize(object.errors.count, "erreur") %>.
    </p>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>